% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_many_maps.R
\name{plot_many_maps}
\alias{plot_many_maps}
\title{Plot Many Maps}
\usage{
plot_many_maps(map_list, labels, size = 2)
}
\arguments{
\item{map_list}{A list of \code{sfc_POLYGON} objects, each containing
regions of a map to be plotted.}

\item{labels}{A character vector containing the labels for the regions of
the \code{sfc_POLYGON} objects.}

\item{size}{numeric. Controls size of labels in plot.}
}
\value{
Prints a plot with labels of the maps in the \code{map_list}
 argument.
}
\description{
Plot many maps of a single area.
}
\details{
Each element of the \code{map_list} argument must have the same number of
features, with the first feature of each element corresponding to the same
region, the second feature of each element corresponding to the same region,
etc. Region labels must be in the same order as the regions of each
\code{sfc_POLYGON} object.
}
\examples{
us_maps <- many_maps(governors$geometry, governors$abbreviation,
                     prop = c(0, 0.1), interpolate = c(0.5, 1),
                     smoothness = c(0, 20), shift = list(c(0,0), c(0,0.5)))
plot_many_maps(us_maps$map, governors$abbreviation, size = 1.5)

}
